import wait from "@/utils/function/wait";

const PopularProducts = async () => {
    await wait();

    return (
        <div className="mt-5">
            Popular Products
        </div>
    );
};

export default PopularProducts;